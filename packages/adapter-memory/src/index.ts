/**
 * @minimalerp/adapter-memory — in-memory implementation of the ports.
 * Test oracle, demo mode and local dev. Must pass the same contract suite as the Supabase adapter.
 */
export { MemoryBackend, assertCommitInvariants, type RevisionRecord } from './memoryBackend';
